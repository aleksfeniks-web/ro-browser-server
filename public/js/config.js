/**
 * WebRO - Client Configuration
 * Configure your Cloud CDN here to host extracted data.grf files.
 */
window.WebROConfig = {
  // URL del CDN de almacenamiento de objetos (ej: Cloudflare R2, Amazon S3, Backblaze B2)
  // DEBE terminar con barra diagonal (/). Deja vacío "" si no tienes CDN y deseas usar la carga local.
  // Ejemplo: "https://pub-abc123xyz.r2.dev/"
  cdnUrl: "" 
};
